% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citest.R
\name{meta}
\alias{meta}
\title{Static metadata describing a test.}
\usage{
meta(test)
}
\arguments{
\item{test}{A \code{citest_test} from one of the factory functions.}
}
\value{
A named list with \code{name} (the stable identifier), \code{data_types}
(\code{"discrete"} and/or \code{"continuous"}), \code{symmetric} (whether swapping \code{x}
and \code{y} leaves the result unchanged), and \code{rule}: \code{"p_value_ge"} for the
standard null-of-independence tests, \code{"p_value_lt"} for equivalence
(TOST) tests, which declare independence when the p-value is \strong{below}
the significance level.
}
\description{
Returns the test's own description of itself, so callers can ask how a
p-value should be interpreted rather than hardcoding which tests use which
convention.
}
\examples{
df <- data.frame(A = sample(0:1, 20, TRUE), B = sample(0:1, 20, TRUE))
meta(chi_squared(dataset(df)))$rule
}
