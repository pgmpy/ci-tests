% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citest.R, R/extendr-wrappers.R
\name{list_tests}
\alias{list_tests}
\title{List every available conditional-independence test.}
\usage{
list_tests()

list_tests()
}
\value{
A list with one entry per test, each a named list as returned by
\code{\link[=meta]{meta()}}: \code{name}, \code{data_types}, \code{symmetric} and \code{rule}.
}
\description{
Reports what the package provides, straight from the core registry, so
callers do not have to hardcode the set of factories or guess how each
test's p-value should be read.

Lets callers discover what is available without hardcoding a list of the
eight factories, and read each test's decision rule rather than assuming it.
}
\examples{
vapply(list_tests(), function(m) m$name, character(1))
}
