name: JS CI

# NOTE: The build/test commands below were verified by running them locally
# against this branch. They are NOT exercised on GitHub Actions until this
# workflow runs there, so the exact runner setup may still need tweaks.
#
# Build: a *real* wasm-pack (installed via jetli/wasm-pack-action) plus the
# wasm32-unknown-unknown target build the package into crates/citest-js/pkg, which
# the vitest suite imports as ../pkg/citest_js.js. `wasm-opt` is disabled via the
# crate's package metadata (crates/citest-js/Cargo.toml) because the locally
# installed wasm-opt is too old to parse the modern wasm the core emits; it only
# shrinks the binary, so disabling it does not affect correctness. CI may
# re-enable it once a newer wasm-opt is guaranteed on the runner.
#
# The root npm script discovers tests/*.test.js; the golden parity suite
# (golden.test.js) covers all 80 fixture cases.

on:
  push:
    branches: [master, development]
    paths:
      - "crates/citest-js/**"
      - "crates/citest/**"
      - "tests/fixtures/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/js.yml"
  pull_request:
    paths:
      - "crates/citest-js/**"
      - "crates/citest/**"
      - "tests/fixtures/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/js.yml"

concurrency:
  group: js-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  js-lint:
    name: JS Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: crates/citest-js/package-lock.json

      - name: Install dependencies
        working-directory: crates/citest-js
        run: npm ci

      - name: Check formatting
        working-directory: crates/citest-js
        run: npx prettier --check .

      - name: Run ESLint
        working-directory: crates/citest-js
        run: npx eslint .

  js-test:
    name: JS Tests
    needs: [js-lint]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v5

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: crates/citest-js/package-lock.json

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      # A real wasm-pack release (not the placeholder installer script).
      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.4.0
        with:
          version: v0.15.0

      # Verified locally: builds into crates/citest-js/pkg (wasm-opt disabled via the
      # crate's package metadata). The vitest suite imports ../pkg/citest_js.js.
      - name: Build WASM package
        run: wasm-pack build crates/citest-js --target nodejs

      # Verified locally: `npm ci && npm test` in crates/citest-js runs the root
      # script, which discovers tests/*.test.js; the golden suite covers all 80
      # fixture cases.
      - name: Install dependencies
        working-directory: crates/citest-js
        run: npm ci

      - name: Run tests
        working-directory: crates/citest-js
        run: npm test
